# 릴리스 노트 (Play Console "이 버전의 새로운 기능" 붙여넣기용)

각 버전의 사용자 대상 변경 요약. 기술 상세는 루트 `CLAUDE.md` 변경 이력 참조.

---

## 1.4 (versionCode 33) — 2026-07-13

이전 prod 빌드: **1.3 / versionCode 32 / 2026-06-27 / commit `bb467f08`**
(그 사이 대부분의 변경은 OTA(runtime "1")로 배포됨 — 본 빌드는 그 누적분을 네이티브에 편입)

### 핵심 변경 (사용자 관점)

- **메시지 전송 안정화** — 앱이 백그라운드로 가며 응답이 유실돼 같은 메시지가 중복 전송되던 문제 해결(멱등 전송). 전송 중·실패·재시도 상태를 말풍선에 인라인 표시.
- **채팅 입력창 키보드 동기화** — 키보드를 열고 닫을 때 입력창이 애니메이션에 맞춰 부드럽게 따라옴(기존엔 키보드가 다 올라온 뒤에야 스냅).
- **채팅 목록 미리보기·안읽음 배지 분리** — 미리보기는 마지막 대화를, 안 읽은 음성 개수는 우측 핑크 배지로. 내가 마지막에 보냈으면 내 메시지가 보이고, 상대 미청취 메시지는 여전히 마스킹.
- **매치 축하 고양이 애니메이션** 🎉 — 매치 성사 시 폭죽 고양이. 설정에서 on/off 가능.
- **알림 딥링크 안정화** — 콜드 스타트(앱 완전 종료) 상태에서 알림을 탭해도 해당 채팅방으로 정확히 이동.
- 사진 업로드(HEIC) 처리, 보이스 한마디 재생바, 설정 스위치 UI 등 개선 및 버그 수정.

### 스토어 "이 버전의 새로운 기능" (붙여넣기)

**한국어 (ko)**
```
• 메시지 전송이 더 안정적으로 — 중복 전송 방지 및 전송 상태 표시
• 키보드를 열고 닫을 때 입력창이 부드럽게 따라와요
• 채팅 목록에서 안 읽은 메시지를 배지로 한눈에
• 매치 성사 시 축하 고양이 애니메이션 🎉 (설정에서 끄기 가능)
• 알림을 탭하면 해당 대화로 정확히 이동
• 사진 업로드(HEIC)·보이스 한마디 재생 개선 및 버그 수정
```

**English (en)**
```
• More reliable messaging — duplicate-send protection and clear send status
• The message bar now follows the keyboard smoothly
• Spot unread messages at a glance with a new chat-list badge
• A celebration cat animation when you match 🎉 (can be turned off in Settings)
• Tapping a notification opens the right conversation
• Photo upload (HEIC) & voice-intro playback improvements, plus bug fixes
```

**日本語 (ja)** — (한국어 뜻 병기)
```
• メッセージ送信がより安定 — 重複送信を防止し送信状態を表示
  (메시지 전송이 더 안정적으로 — 중복 전송 방지 및 전송 상태 표시)
• キーボードの開閉に合わせて入力欄がなめらかに追従
  (키보드 열고 닫을 때 입력창이 부드럽게 따라옴)
• チャット一覧で未読メッセージがバッジで一目に
  (채팅 목록에서 안 읽은 메시지를 배지로 한눈에)
• マッチ成立時にお祝いのねこアニメーション🎉（設定でオフ可能）
  (매치 성사 시 축하 고양이 애니메이션 🎉, 설정에서 끄기 가능)
• 通知をタップすると該当のトークへ正しく移動
  (알림을 탭하면 해당 대화로 정확히 이동)
• 写真アップロード(HEIC)・ボイス一言の再生を改善し不具合を修正
  (사진 업로드(HEIC)·보이스 한마디 재생 개선 및 버그 수정)
```
